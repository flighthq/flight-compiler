// Generated by @flighthq/tool-compiler from @flighthq/golden/packages/golden/src/doWhileLoop.ts#DoWhileLoop. Do not edit.
#![forbid(unsafe_code)]

pub fn collatz(n: f64) -> f64 {
  let mut steps: f64 = 0.0;
  let mut current: f64 = n;
  while current != 1.0 {
    if (current - (2.0 * f64::floor((current / 2.0)))) == 0.0 {
      current = current / 2.0;
    }
    else {
      current = (3.0 * current) + 1.0;
    }
    steps = steps + 1.0;
  }
  return steps;
}

pub fn digit_sum(n: f64) -> f64 {
  let mut sum: f64 = 0.0;
  let mut remaining: f64 = f64::abs(n);
  while remaining > 0.0 {
    sum = sum + (remaining - (10.0 * f64::floor((remaining / 10.0))));
    remaining = f64::floor((remaining / 10.0));
  }
  return sum;
}

pub fn gcd(a: f64, b: f64) -> f64 {
  let mut x: f64 = f64::abs(a);
  let mut y: f64 = f64::abs(b);
  while y != 0.0 {
    let temp: f64 = y;
    y = x - (y * f64::floor((x / y)));
    x = temp;
  }
  return x;
}
