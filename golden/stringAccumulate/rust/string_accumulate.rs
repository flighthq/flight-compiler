// Generated by @flighthq/tool-compiler from @flighthq/golden/packages/golden/src/stringAccumulate.ts#StringAccumulate. Do not edit.
#![forbid(unsafe_code)]

pub fn join(parts: Vec<String>, separator: String) -> String {
  if (parts.len() as f64) == 0.0 {
    return "".to_owned();
  }
  let mut result: String = parts[0].clone();
  {
    let mut i: f64 = 1.0;
    while i < (parts.len() as f64) {
      {
        result = format!("{}{}{}", result, separator, parts[i as usize]);
      }
      i += 1.0;
    }
  }
  return result;
}

pub fn repeat(text: String, count: f64) -> String {
  let mut result: String = "".to_owned();
  {
    let mut i: f64 = 0.0;
    while i < count {
      {
        result = format!("{}{}", result, text);
      }
      i += 1.0;
    }
  }
  return result;
}

pub fn wrap(text: String, before: String, after: String) -> String {
  return format!("{}{}{}", before, text, after);
}

pub fn pad_left(text: String, width: f64, fill: String) -> String {
  let mut result: String = text;
  while (result.len() as f64) < width {
    result = format!("{}{}", fill, result);
  }
  return result;
}
