// Generated by @flighthq/tool-compiler from @flighthq/golden/packages/golden/src/enumSwitch.ts#EnumSwitch. Do not edit.
package flighthq.golden;

import Reflect as HaxeReflect;

enum abstract Direction(String) from String to String {
  var Up = "up";
  var Down = "down";
  var Left = "left";
  var Right = "right";
}

function isVertical(dir:Direction):Bool {
  return flighthq._internal._Js.strictEqual(dir, Direction.Up) || flighthq._internal._Js.strictEqual(dir, Direction.Down);
}

function opposite(dir:Direction):Direction {
  {
    final switchSubject = dir;
    if (js.Syntax.strictEq(switchSubject, Direction.Up)) {
      return Direction.Down;
    }
    else if (js.Syntax.strictEq(switchSubject, Direction.Down)) {
      return Direction.Up;
    }
    else if (js.Syntax.strictEq(switchSubject, Direction.Left)) {
      return Direction.Right;
    }
    else if (js.Syntax.strictEq(switchSubject, Direction.Right)) {
      return Direction.Left;
    }
  }
  return cast(js.Syntax.code("undefined"));
}

function directionLabel(dir:Direction):String {
  {
    final switchSubject_2 = dir;
    if (js.Syntax.strictEq(switchSubject_2, Direction.Up)) {
      return "north";
    }
    else if (js.Syntax.strictEq(switchSubject_2, Direction.Down)) {
      return "south";
    }
    else if (js.Syntax.strictEq(switchSubject_2, Direction.Left)) {
      return "west";
    }
    else if (js.Syntax.strictEq(switchSubject_2, Direction.Right)) {
      return "east";
    }
  }
  return cast(js.Syntax.code("undefined"));
}
