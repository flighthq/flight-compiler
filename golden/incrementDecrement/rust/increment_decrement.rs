// Generated by @flighthq/tool-compiler from @flighthq/golden/packages/golden/src/incrementDecrement.ts#IncrementDecrement. Do not edit.
#![forbid(unsafe_code)]

pub fn count_up(start: f64, steps: f64) -> f64 {
  let mut value: f64 = start;
  {
    let mut i: f64 = 0.0;
    while i < steps {
      {
        value += 1.0;
      }
      i += 1.0;
    }
  }
  return value;
}

pub fn count_down(start: f64, steps: f64) -> f64 {
  let mut value: f64 = start;
  {
    let mut i: f64 = 0.0;
    while i < steps {
      {
        value -= 1.0;
      }
      i += 1.0;
    }
  }
  return value;
}

pub fn step_accumulate(n: f64) -> f64 {
  let mut total: f64 = 0.0;
  let mut step: f64 = 1.0;
  {
    let mut i: f64 = 0.0;
    while i < n {
      {
        total += step;
        step += 2.0;
      }
      i += 1.0;
    }
  }
  return total;
}

pub fn halving_steps(value: f64) -> f64 {
  let mut count: f64 = 0.0;
  let mut current: f64 = value;
  while current > 1.0 {
    current = f64::floor((current / 2.0));
    count += 1.0;
  }
  return count;
}
