// Generated by @flighthq/tool-compiler from @flighthq/golden/packages/golden/src/stringCharCode.ts#StringCharCode. Do not edit.
#![forbid(unsafe_code)]

pub fn char_code_first(text: String) -> f64 {
  return text.chars().nth(0.0 as usize).map(|c| c as u32 as f64).unwrap_or(f64::NAN);
}

pub fn is_digit_code(text: String, index: f64) -> bool {
  let code: f64 = text.chars().nth(index as usize).map(|c| c as u32 as f64).unwrap_or(f64::NAN);
  return (code >= 48.0) && (code <= 57.0);
}

pub fn sum_char_codes(text: String) -> f64 {
  let mut total: f64 = 0.0;
  {
    let mut i: f64 = 0.0;
    while i < (text.len() as f64) {
      {
        total = total + text.chars().nth(i as usize).map(|c| c as u32 as f64).unwrap_or(f64::NAN);
      }
      i = i + 1.0;
    }
  }
  return total;
}
