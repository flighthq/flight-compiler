// Generated by @flighthq/tool-compiler from @flighthq/golden/packages/golden/src/whileSearch.ts#WhileSearch. Do not edit.
#![forbid(unsafe_code)]

pub fn first_above(values: Vec<f64>, threshold: f64) -> f64 {
  let mut i: f64 = 0.0;
  while i < (values.len() as f64) {
    if values[i as usize] > threshold {
      return values[i as usize];
    }
    i += 1.0;
  }
  return -1.0;
}

pub fn count_occurrences(text: String, target: String) -> f64 {
  let mut count: f64 = 0.0;
  let mut pos: f64 = 0.0;
  while pos < (text.len() as f64) {
    if text.chars().nth(pos as usize).map(|c| c.to_string()).unwrap_or_default() == target {
      count += 1.0;
    }
    pos += 1.0;
  }
  return count;
}

pub fn index_of_first(values: Vec<f64>, target: f64) -> f64 {
  let mut i: f64 = 0.0;
  while i < (values.len() as f64) {
    if values[i as usize] == target {
      return i;
    }
    i += 1.0;
  }
  return -1.0;
}
