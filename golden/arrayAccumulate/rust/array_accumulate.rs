// Generated by @flighthq/tool-compiler from @flighthq/golden/packages/golden/src/arrayAccumulate.ts#ArrayAccumulate. Do not edit.
#![forbid(unsafe_code)]

pub fn running_sum(values: Vec<f64>) -> Vec<f64> {
  let mut result: Vec<f64> = vec![];
  let mut sum: f64 = 0.0;
  for value in values {
    sum = sum + value;
    result.push(sum);
  }
  return result;
}

pub fn filter_positive(values: Vec<f64>) -> Vec<f64> {
  let mut result: Vec<f64> = vec![];
  for value in values {
    if value > 0.0 {
      result.push(value);
    }
  }
  return result;
}

pub fn sum_squares(values: Vec<f64>) -> f64 {
  let mut total: f64 = 0.0;
  for value in values {
    total = total + (value * value);
  }
  return total;
}
