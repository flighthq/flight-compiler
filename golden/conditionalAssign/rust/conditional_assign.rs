// Generated by @flighthq/tool-compiler from @flighthq/golden/packages/golden/src/conditionalAssign.ts#ConditionalAssign. Do not edit.
#![forbid(unsafe_code)]

pub fn sign(value: f64) -> String {
  let mut label: String;
  if value > 0.0 {
    label = "positive".to_owned();
  }
  else {
    if value < 0.0 {
      label = "negative".to_owned();
    }
    else {
      label = "zero".to_owned();
    }
  }
  return label;
}

pub fn bounds(values: Vec<f64>) -> f64 {
  let mut min: f64 = values[0].clone();
  let mut max: f64 = values[0].clone();
  for v in values {
    if v < min {
      min = v;
    }
    if v > max {
      max = v;
    }
  }
  return max - min;
}

pub fn grade(score: f64) -> String {
  let mut letter: String;
  if score >= 90.0 {
    letter = "A".to_owned();
  }
  else {
    if score >= 80.0 {
      letter = "B".to_owned();
    }
    else {
      if score >= 70.0 {
        letter = "C".to_owned();
      }
      else {
        if score >= 60.0 {
          letter = "D".to_owned();
        }
        else {
          letter = "F".to_owned();
        }
      }
    }
  }
  return letter;
}
