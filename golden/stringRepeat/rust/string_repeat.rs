// Generated by @flighthq/tool-compiler from @flighthq/golden/packages/golden/src/stringRepeat.ts#StringRepeat. Do not edit.
#![forbid(unsafe_code)]

pub fn repeat_string(text: String, count: f64) -> String {
  let mut result: String = "".to_owned();
  {
    let mut i: f64 = 0.0;
    while i < count {
      {
        result = format!("{}{}", result, text);
      }
      i += 1.0;
    }
  }
  return result;
}

pub fn join_with_separator(items: Vec<String>, separator: String) -> String {
  if (items.len() as f64) == 0.0 {
    return "".to_owned();
  }
  let mut result: String = items[0];
  {
    let mut i: f64 = 1.0;
    while i < (items.len() as f64) {
      {
        result = format!("{}{}{}", result, separator, items[i as usize]);
      }
      i += 1.0;
    }
  }
  return result;
}

pub fn pad_left(text: String, width: f64, fill: String) -> String {
  let mut result: String = text;
  while (result.len() as f64) < width {
    result = format!("{}{}", fill, result);
  }
  return result;
}

pub fn surround_with(text: String, left: String, right: String) -> String {
  return format!("{}{}{}", left, text, right);
}
