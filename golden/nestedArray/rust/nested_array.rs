// Generated by @flighthq/tool-compiler from @flighthq/golden/packages/golden/src/nestedArray.ts#NestedArray. Do not edit.
#![forbid(unsafe_code)]

pub fn sum_row(grid: Vec<Vec<f64>>, row: f64) -> f64 {
  let mut total: f64 = 0.0;
  for value in grid[row as usize] {
    total += value;
  }
  return total;
}

pub fn flatten(grid: Vec<Vec<f64>>) -> Vec<f64> {
  let mut result: Vec<f64> = vec![];
  for row in grid {
    for value in row {
      result.push(value);
    }
  }
  return result;
}

pub fn transpose(grid: Vec<Vec<f64>>) -> Vec<Vec<f64>> {
  let rows: f64 = grid.len() as f64;
  let cols: f64 = grid[0].len() as f64;
  let mut result: Vec<Vec<f64>> = vec![];
  {
    let mut c: f64 = 0.0;
    while c < cols {
      {
        let mut row: Vec<f64> = vec![];
        {
          let mut r: f64 = 0.0;
          while r < rows {
            {
              row.push(grid[r as usize][c as usize]);
            }
            r += 1.0;
          }
        }
        result.push(row);
      }
      c += 1.0;
    }
  }
  return result;
}
