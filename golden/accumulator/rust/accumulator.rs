// Generated by @flighthq/tool-compiler from @flighthq/golden/packages/golden/src/accumulator.ts#Accumulator. Do not edit.
#![forbid(unsafe_code)]

pub fn sum_until(values: Vec<f64>, limit: f64) -> f64 {
  let mut total: f64 = 0.0;
  for value in values {
    if (total + value) > limit {
      break;
    }
    total = total + value;
  }
  return total;
}

pub fn count_positive(values: Vec<f64>) -> f64 {
  let mut count: f64 = 0.0;
  for value in values {
    if value > 0.0 {
      count = count + 1.0;
    }
  }
  return count;
}

pub fn find_first_negative(values: Vec<f64>) -> f64 {
  for value in values {
    if value < 0.0 {
      return value;
    }
  }
  return 0.0;
}
